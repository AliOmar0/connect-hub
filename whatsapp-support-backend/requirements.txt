fastapi>=0.109.0
uvicorn[standard]>=0.27.0
supabase>=2.3.0
pydantic>=2.5.3
pydantic-settings>=2.1.0
httpx>=0.26.0
python-dotenv>=1.0.1
flask
flask-cors
requests
torch
transformers
accelerate
librosa
soundfile
pyngrok

# --- NLP pipeline (intent / language / entity) ---
# Runtime intent classification uses torch + transformers above when a
# fine-tuned checkpoint is configured (INTENT_MODEL_PATH); otherwise the
# dependency-free heuristic fallback is used. The following are only needed to
# (re)train the intent model via scripts/train_intent_classifier.py:
#   datasets>=2.16
#   scikit-learn>=1.3
# And, optionally, CAMeL-Tools for richer Arabic NER.
